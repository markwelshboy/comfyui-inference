# Keep pip happy with common wheels
numpy==2.2.6

# Avoid surprise hf hub major jumps (you already like this)
huggingface-hub==0.36.0

# Click pin if you still install comfy-cli anywhere
click<=8.1.8

# Optional: keep opencv/numpy compatibility stable
opencv-python-headless==4.12.0.88

# If you use kornia/scipy a lot:
kornia==0.8.2
scipy==1.17.0